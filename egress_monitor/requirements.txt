# Local dev / unit-test deps. The serverless job declares its own runtime deps
# (databricks-sdk, pydantic-settings) in resources/egress_monitor.job.yml; the
# runtime provides pyspark. This file is for running the unit tests locally.
pydantic-settings>=2.0
databricks-sdk>=0.20
pyyaml>=6.0
pytest>=7.0
